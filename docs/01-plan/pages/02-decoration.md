# 선물꾸미기 페이지 기획서

> PDF p.15~19 기반

---

## 개요

- **경로**: `/decoration`, `/decoration/create`, `/decoration/:id`
- **메뉴**: 사이드바 2번
- **목적**: 쿠폰 상품 MMS에 포함되는 배너와 템플릿 등록/관리

---

## 페이지 목록

| 경로 | 페이지명 | 설명 |
|------|---------|------|
| `/decoration` | 선물꾸미기 목록 | 배너/템플릿 목록 조회 |
| `/decoration/create` | 배너/템플릿 등록 | 신규 배너/템플릿 등록 |
| `/decoration/:id` | 배너/템플릿 수정 | 기존 배너/템플릿 수정 |

---

## 1. 선물꾸미기 목록

### 화면 구성

```
┌─────────────────────────────────────────────────────────────────┐
│ 선물 꾸미기                                                      │  ← PageHeader
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 사용여부 [전체 V]   구분 [전체 V]   제목 [__________]         │ │  ← SearchFilter
│ │                                    ☐ 기본값만 보기           │ │
│ │                                           [초기화] [검색]   │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│                           [카테고리 등록] [배너/템플릿 등록]       │  ← 액션 버튼 영역
│                                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ID│구분│카테고리│이미지│제목│기본값│사용여부│등록일시│업데이트일시│ │  ← Table
│ ├──┼───┼──────┼────┼───┼────┼─────┼──────┼────────┤ │
│ │1112│배너│기본│[img]│네이트온(기본)│Y│사용│2025.12.22│2025.12.22│ │  ← 행 클릭 시 수정
│ │1111│배너│시즌│[img]│크리스마스│-│사용│2025.12.26│2025.12.26│ │
│ │1110│템플릿│기본│[img]│네이트온(기본)│Y│사용│2025.12.22│2025.12.22│ │
│ │1109│템플릿│축하│[img]│산타│-│미사용│2025.12.26│2025.12.26│ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│                        [페이지네이션]                            │
└─────────────────────────────────────────────────────────────────┘
```

### 검색 필터

| 필드명 | 컴포넌트 | 옵션/플레이스홀더 | 검색 방식 |
|--------|----------|------------------|----------|
| 사용여부 | Select | 전체 / 사용 / 미사용 | exact |
| 구분 | Select | 전체 / 배너 / 템플릿 | exact |
| 제목 | Input | "제목" | like %검색어% |
| 기본값만 보기 | Checkbox | - | isDefault === true |

### 액션 버튼

| 버튼명 | Variant | 동작 |
|--------|---------|------|
| 카테고리 등록 | primary | 배너/템플릿 카테고리 관리 팝업 열기 |
| 배너/템플릿 등록 | primary | `/decoration/create`로 이동 |

### 테이블 정의

| 컬럼명 | 필드 | 타입 | 너비 | 정렬 |
|--------|------|------|------|------|
| ID | id | number | 60px | left |
| 구분 | type | string (배너/템플릿) | 70px | left |
| 카테고리 | categoryName | string | 80px | left |
| 이미지 | imageUrl | image (썸네일) | 100px | center |
| 제목 | title | string | auto | left |
| 기본값 | isDefault | string (Y/-) | 60px | center |
| 사용여부 | isActive | string (사용/미사용) | 70px | left |
| 등록일시 | createdAt | datetime | 130px | left |
| 업데이트일시 | updatedAt | datetime | 130px | left |

### 테이블 동작

- **행 클릭**: `/decoration/:id`로 이동 (수정 화면)
- **정렬**: 최신 ID순 (내림차순)
- **페이지네이션**: 20개씩

---

## 2. 배너/템플릿 카테고리 관리 팝업

> [카테고리 등록] 버튼 클릭 시 Modal로 열림
> ※ 전시 카테고리 관리 팝업과 다르게 **아이콘 컬럼 없음**

### 조회 모드

```
┌─────────────────────────────────────────────────────────┐
│ 배너/템플릿 카테고리 관리                            [X] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 총 4개                                         [편집]   │
│                                                         │
│ ┌─────┬────────────┬──────────┐                        │
│ │ id  │ 카테고리명  │ 사용여부  │                        │
│ ├─────┼────────────┼──────────┤                        │
│ │1222 │   기본      │   사용   │                        │
│ │1221 │   시즌      │   사용   │                        │
│ │1220 │   축하      │   사용   │                        │
│ │1219 │   경조      │  미사용  │                        │
│ └─────┴────────────┴──────────┘                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 편집 모드

```
┌─────────────────────────────────────────────────────────┐
│ 배너/템플릿 카테고리 관리                            [X] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 총 4개                                       [+ 추가]   │
│                                                         │
│ ┌─────┬────────────────────┬─────────────────┐         │
│ │ id  │    카테고리명       │    사용여부     │         │
│ ├─────┼────────────────────┼─────────────────┤         │
│ │     │ [카테고리명을 입력] │ ● 사용 ○ 미사용 │         │ ← 신규 행
│ │1222 │ [기본_________]    │ ● 사용 ○ 미사용 │         │
│ │1221 │ [시즌_________]    │ ● 사용 ○ 미사용 │         │
│ │...  │       ...         │      ...        │         │
│ └─────┴────────────────────┴─────────────────┘         │
│                                                         │
│                               [취소]  [저장]            │
└─────────────────────────────────────────────────────────┘
```

### 팝업 테이블 컬럼

| 컬럼명 | 조회모드 | 편집모드 |
|--------|---------|---------|
| id | text (자동생성) | text (비활성화) |
| 카테고리명 | text | Input |
| 사용여부 | text (사용/미사용) | Radio (사용/미사용) |

### 유효성 검사

- 카테고리명 중복 불가 → alert "중복된 이름의 카테고리가 존재합니다."
- 해당 카테고리로 배너/템플릿이 등록되어 있는데 미사용으로 변경 시 → alert "{카테고리명}에 등록된 배너 또는 템플릿이 존재하여 미사용으로 수정이 불가능합니다."

---

## 3. 배너/템플릿 등록/수정

### 화면 구성

```
┌─────────────────────────────────────────────────────────────────┐
│ [← 목록으로]  배너/템플릿 등록                                    │  ← PageHeader
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                                                             │ │
│ │  구분 *              카테고리 *                              │ │
│ │  [선택         V]    [선택         V]                        │ │
│ │                                                             │ │
│ │  제목(관리용) *                                              │ │
│ │  [관리용 제목을 입력하세요_______________________]            │ │
│ │                                                             │ │
│ │  이미지 *                        미리보기                    │ │
│ │  ┌─────────────────┐            ┌─────────────────┐         │ │
│ │  │                 │            │                 │         │ │
│ │  │    [📤 업로드]   │            │  이미지를       │ [🗑]    │ │
│ │  │                 │            │  업로드하세요    │         │ │
│ │  └─────────────────┘            └─────────────────┘         │ │
│ │  ※ 권장 사이즈 가이드 안내                                    │ │
│ │                                                             │ │
│ │  ☐ 기본값으로 노출                                           │ │
│ │                                                             │ │
│ │  사용여부                                                    │ │
│ │  ● 사용  ○ 미사용                                           │ │
│ │                                                             │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│                                        [취소]  [저장]           │
└─────────────────────────────────────────────────────────────────┘
```

### 폼 필드 정의

| 필드명 | 컴포넌트 | 필수 | 옵션/설명 |
|--------|----------|------|----------|
| 구분 | Select | Y | 선택 / 배너 / 템플릿 |
| 카테고리 | Select | Y | (동적 - 사용 중인 카테고리 목록) |
| 제목(관리용) | Input | Y | 관리용 제목 (동일 구분 내 중복 불가) |
| 이미지 | ImageUpload | Y | 1개만 업로드 가능 |
| 미리보기 | - | - | 업로드한 이미지 미리보기 + 삭제 버튼 |
| 기본값으로 노출 | Checkbox | N | 체크 시 서비스상 디폴트 이미지로 노출 |
| 사용여부 | Radio | N | 사용 / 미사용 (기본값: 사용) |

### 액션 버튼

| 버튼 | 동작 |
|------|------|
| ← 목록으로 | `/decoration`으로 이동 |
| 취소 | `/decoration`으로 이동 |
| 저장 | 저장 후 `/decoration`으로 이동 |

### 유효성 검사

- 필수값 미입력 시 저장 불가
- 제목 중복 (동일 구분 내) → alert "중복된 이름이 존재합니다."

### 수정 화면 차이점

- PageHeader 제목: "배너/템플릿 수정"
- 기존 데이터 로드하여 폼에 표시
- 이미지 영역에 기존 이미지 미리보기 표시

---

## 배너/템플릿 노출 위치 (서비스 화면 참고)

> PDF p.19 참고

```
┌─────────────────────────┐
│        주문 정보         │
├─────────────────────────┤
│                         │
│  선물 꾸미기             │
│  ┌───────────────────┐  │
│  │                   │  │
│  │   1. 배너 영역     │  │  ← 배너 이미지
│  │                   │  │
│  │  ┌─────────────┐  │  │
│  │  │             │  │  │
│  │  │ 2. 템플릿   │  │  │  ← 템플릿 이미지
│  │  │             │  │  │
│  │  └─────────────┘  │  │
│  └───────────────────┘  │
│                         │
└─────────────────────────┘
```

---

## 데이터 모델

### 배너/템플릿 카테고리 (decorationCategory)

```typescript
interface DecorationCategory {
  id: number;                    // 카테고리 ID
  name: string;                  // 카테고리명
  isActive: boolean;             // 사용여부
  createdAt: string;             // 등록일시
  updatedAt: string;             // 수정일시
}
```

### 배너/템플릿 (decoration)

```typescript
interface Decoration {
  id: number;                    // ID
  type: 'banner' | 'template';   // 구분
  categoryId: number;            // 카테고리 ID
  categoryName: string;          // 카테고리명 (조회용)
  title: string;                 // 제목(관리용)
  imageUrl: string;              // 이미지 URL
  isDefault: boolean;            // 기본값 여부
  isActive: boolean;             // 사용여부
  createdAt: string;             // 등록일시
  updatedAt: string;             // 수정일시
}
```

---

## 비즈니스 규칙

1. **기본값으로 노출**
   - 체크 시 서비스상 선물꾸미기의 디폴트 이미지로 보여짐
   - 사용 중인 다수의 항목이 기본값 체크되어 있으면 최신 ID가 디폴트

2. **이미지 업로드**
   - 1개만 업로드 가능
   - 업로드 후 삭제 버튼으로 삭제하고 재업로드 가능
   - 이미지 사이즈/확장자/용량은 디자이너 의견에 따름 (※ 권장 사이즈 가이드 표시)

---

## 사용 컴포넌트

- PageHeader
- SearchFilter
- Button (primary, secondary, text)
- Select
- Input
- Checkbox
- Radio
- Table
- Modal
- ImageUpload
- Card
- FormField
